<% hide_left %>
<% hide_right %>
<h1><%= title "Email Friends and Family" %></h1>

<% form_for :email, :url => send_email_donation_page_path do |f| %>

  <p>
    <%= f.label :recipients, "Recipient(s)" %><br />
    If you would like to email more than one person just enter the emails in a comma seperated list (e.g. friend@email.com, family@email.co.uk).<br />
    <%= f.text_field :recipients, :value => params[:email][:recipients] %><br />
  </p>  
  
  <p>
    <%= f.label :subject %>
    <br />
    This is the actual subject line in your email - we recommend you also include your full name so the receiver will recognise it's from you.
    <br />
    <%= f.text_field :subject, :value => params[:email][:subject] %>
  </p>
  
  <p>
    <%= f.label :content %><br />
    <%= f.text_area :content, :value => params[:email][:content] %>
  </p>

  <p><%= f.submit "Send Email" %></p>

<% end -%>
