<% form_for [:admin, @product_image], :html => {:multipart => true} do |f| %>
  <%= f.error_messages %>

  <p>
    <%= image_field_tag f, :product_image, :image %>
  </p>
  
  <%= f.hidden_field :product_id %>

  <p><%= f.submit "Submit" %></p>
<% end %>
