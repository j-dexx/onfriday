<% form_for [:admin, @shipping_option] do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :price, "Price (&pound;)" %><br />
    <%= f.text_field :price %>
  </p>

  <p>
    <%= f.label :region %><br />
    <%= f.select :region, options_for_select(Shipping.regions, f.object.region) %>
  </p>

  <p><%= f.submit "Submit" %></p>
<% end %>
